{
  "name": "uyc-webcam-events-refresh",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8788 --persist-to ../../.wrangler-local/events --var ENVIRONMENT:local --var EVENTS_OBJECT_KEY:var/events7day.json",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
